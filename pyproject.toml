[project]
name = "py-ai-agent"
version = "0.1.0"
description = "A simple toy AI agent based on Gemini Flash"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "google-genai==1.12.1",
    "python-dotenv==1.1.0",
]
